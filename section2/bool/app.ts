export { };

let pending: boolean;
pending = true;
console.log(pending);
// JavaScript has the Boolean type that refers to the non-primitive boxed object. 
// The Boolean type has the letter B in uppercase, which is different from the boolean type.

pending = false;
console.log(pending);